export interface OrderItemEntity {
    id: string
    productId: string
    quantity: number
    price: string
}

export interface OrderItemDTO {
    id: string
    productId: string
    quantity: number
    price: string
}
